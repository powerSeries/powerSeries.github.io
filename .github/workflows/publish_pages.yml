name: "publish-pages"
on:
    pull_request_review:
        types:
            - submitted

jobs:
    if_merged:
        if: ${{ github.event.pull_request.merged }}
        runs-on: unbuntu-latest
        steps:
            - run: echo The PR was merged

